# Codex project configuration

- Put custom Codex subagent definitions in `agents/*.toml`.
- Add project-level Codex settings in `config.toml` only when a shared setting is needed.
- Put Codex skills in `.agents/skills`, not `.codex/skills`.
- Keep behavioral project instructions in the root or nearest nested `AGENTS.md`.
