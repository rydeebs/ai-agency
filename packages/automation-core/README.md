# Automation core

Future home for reusable workflow primitives: steps, triggers, retries, idempotency keys, approval gates, run state, cancellation, and replay behavior.

Client-specific workflow composition belongs in the automation worker or a dedicated private client deployment, not in this generic package.
