# Database package

Future home for the database schema, migrations, generated client, seed utilities, and tenant-aware repositories.

Keep migrations append-only after production use. Enforce tenant isolation and least-privilege access in the data layer rather than relying only on UI filters.
