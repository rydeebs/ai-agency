# AI package

Future home for provider-neutral model access, prompt templates, structured-output schemas, safety policies, evaluation fixtures, token/cost tracking, and fallback behavior.

Prompts that make business decisions should be versioned and tested. Client data must be minimized before it reaches any model provider.
