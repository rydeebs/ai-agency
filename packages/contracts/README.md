# Contracts package

Future source of truth for shared TypeScript types and runtime schemas used across application boundaries: leads, companies, contacts, engagements, workflow runs, webhooks, audit events, and integration commands.

Runtime validation is required; TypeScript types alone do not validate network or database input.
