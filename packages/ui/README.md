# UI package

Future home for genuinely shared interface components and design tokens used by the CRM or other authenticated apps.

The marketing website can keep its specialized presentation components locally. Move a component here only when a second app needs the same behavior and visual contract.
