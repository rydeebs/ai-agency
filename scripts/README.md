# Repository scripts

This directory is for repository-wide, repeatable development and operational commands. App-specific scripts remain with their app.

Scripts should be non-interactive where practical, fail clearly, avoid embedding credentials, and document any external side effects.
